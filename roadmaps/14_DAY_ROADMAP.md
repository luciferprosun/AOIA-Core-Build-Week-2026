# AOIA reviewer roadmap — next 14 days

> **PLAN ONLY — NOT IMPLEMENTED, COMMITTED, TESTED, RELEASED, OR PROMISED**

This reviewer-facing plan starts from the synchronized production evidence boundary at AOIA-Core commit `7f61a3b167a028d7e34b852cca4ade809ceec571`. It does not extend that commit's implementation status and grants no execution, write, provider, network, release, or approval authority.

## Evidence-backed starting point

- The source branch has a human-controlled operator cockpit, deterministic safety evidence, fail-closed write controls, and test-protected authority boundaries at the frozen source HEAD.
- The Critical Prompt Loop desktop demonstration and the proposed German Worker Law Knowledge HAT are separate workstreams outside this production-report snapshot.
- A knowledge result, observer report, score, test, manifest, preview, or roadmap item remains non-authoritative.

## Days 1–2 — reproduce the baseline

- Validate an isolated clean clone at the exact source HEAD.
- Reproduce the documented focused and full-suite checks without changing the frozen evidence.
- Reconcile runtime, reviewer documentation, and committed manifest identities.

Exit evidence: a human-reviewed reproducibility note tied to an exact commit and commands. A passing check does not authorize release.

## Days 3–4 — specify one generic read-only HAT bridge

- Define a minimal adapter contract for bounded, structured evidence retrieval.
- Keep source identity, effective date, section/document reference, excerpt bounds, and retrieval metadata explicit.
- Define fail-closed unavailable and retrieval-failure states.
- Prohibit provider access, writes, execution, approval, dispatch, and implicit authority in the adapter.

Exit evidence: reviewed interface and threat-boundary proposal. No HAT is considered connected at this stage.

## Days 5–7 — connect one German employment-law pilot behind the contract

- Reuse the existing local read-only German-law capability only after its corpus and public interface pass provenance review.
- Attach a maximum of a few bounded evidence items marked `LOCAL DOMAIN KNOWLEDGE — EVIDENCE ONLY`.
- Keep `No HAT` available and make missing or failed retrieval visibly unavailable.
- Do not copy the legal corpus into application or reviewer repositories.

Exit evidence: focused adapter tests and a reviewer-approved local smoke result. This is not legal advice or a legal-decision system.

## Days 8–10 — validate independent and combined paths

- Test normal send with no HAT.
- Test the HAT path with the observer loop disabled.
- Test HAT evidence propagation through the sequential observer/finalization path when that loop is available.
- Prove that only the primary model's final suggestion can enter the conversation and that observer/HAT metadata cannot become authority.

Exit evidence: deterministic call-order, evidence-bounding, failure, and transcript tests.

## Days 11–12 — freshness and adversarial evaluation

- Create dated test cases for superseded values, missing effective dates, conflicting sources, empty retrieval, malformed records, and stale corpus state.
- Require source-level citations and explicit uncertainty when evidence is insufficient.
- Review privacy, licensing, corpus provenance, and prompt-injection handling.

Exit evidence: bounded evaluation report with failures retained, not hidden.

## Days 13–14 — human release decision

- Run a clean recording rehearsal from a pinned commit.
- Review claims against the implementation and the synchronized report index.
- Confirm secrets, local paths, raw corpora, and private sessions are absent from the reviewer pack.
- Make a separate human go/no-go decision for any commit, push, release, or public demo.

Exit evidence: either a clearly identified release candidate or an explicit blocked/not-ready report.

## Non-goals

- No autonomous routing, approval, execution, fallback, retry, streaming, browser action, or corpus rebuilding.
- No claim that planned German-law integration is already implemented.
- No replacement for current official sources or qualified legal review.
